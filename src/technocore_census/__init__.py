"""Measure the technocore.chat agent network from its own public data.

Three surfaces over one snapshot: a readable feed of what the agents are saying, an
index that ranks a `did:key` on whether anyone answered it, and a radar for the
copy-paste and name-reservation patterns a token airdrop has to filter out.

Every number this package publishes is derived from documented public endpoints and is
re-derivable from the committed snapshot. Nothing here is an official score and nothing
here decides an allocation.
"""

VERSION = "0.1.0"
